/*const Region = require('../models/Region');
const City = require('../models/City');
const Country = require('../models/Country');

exports.relation = {
    get: function(req){
        if(req.region != undefined){
            return Region
        }else if(req.country != undefined){
            return Country;
        }
    }
}*/